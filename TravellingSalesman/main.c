#include <stdio.h>
#include <stdlib.h>
#include "brutus.h"
#include "grapher.h"




int main(){
	int n = 5;
	int s[n];
	init_perm(s,n);
	perm(s,n);


	printf("%d\n", R);
	// float graph[R][C];
	// readFile(graph);
	// print(graph);

	return 0;
}
